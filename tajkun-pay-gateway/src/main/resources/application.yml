server:
  port: 9000
spring:
  application:
    name: tajkun-pay-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/